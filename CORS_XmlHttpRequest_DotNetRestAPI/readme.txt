this sample is to see how to use 'xmlHttpRequest' to call Cross Origin api; also a recap of how to write rest api (get, post) based on .NET framework; 
--------------------------------------------
1. xmlHttpRequestRestAPI.html as client. 
   use script there to new a 'xmlHttpRequest' object to do 'get','post' to rest api hold in .NET project.

2. MemoWebApi is a website, contain front-end site & rest api. 

3. CORS is supported in MemoWebApi rest api.